[project]
name = "chess-crawl"
version = "0.1.0"
description = "Local-first Chess.com public game-history graph crawler"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
chess-crawl = "chess_crawl.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
